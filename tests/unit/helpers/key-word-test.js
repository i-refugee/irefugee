import { keyWord } from '../../../helpers/key-word';
import { module, test } from 'qunit';

module('Unit | Helper | key word');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = keyWord([42]);
  assert.ok(result);
});
